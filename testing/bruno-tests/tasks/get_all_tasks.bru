meta {
  name: get_all_tasks
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/tasks/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("should get tasks successfully", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()).to.be.an('array');
  });
}
