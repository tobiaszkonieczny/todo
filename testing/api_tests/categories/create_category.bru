meta {
  name: create_category
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/categories/new
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Optional tasks"
  }
}

settings {
  encodeUrl: true
}
